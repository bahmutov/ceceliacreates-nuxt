{
  "name": "ceceliacreates-nuxt",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt start",
    "local": "start-test dev 3000 cy:open",
    "serve": "ws --directory dist",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "e2e:open": "start-test serve 8000 'cypress open --config baseUrl=http://localhost:8000'",
    "e2e:run": "start-test serve 8000 'cypress run --config baseUrl=http://localhost:8000'"
  },
  "dependencies": {
    "@nuxt/content": "^1.9.0",
    "cypress": "^6.4.0",
    "nuxt": "^2.14.6",
    "remark-gfm": "^1.0.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.2",
    "local-web-server": "4.2.1",
    "netlify-plugin-cypress": "git+https://github.com/erezrokah/netlify-plugin-cypress.git#feat/use_chrome",
    "start-server-and-test": "1.12.0"
  }
}
