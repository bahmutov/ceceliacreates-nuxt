{
  "name": "ceceliacreates-nuxt",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt start",
    "local": "start-test dev 3000 cy:open",
    "serve": "ws --directory dist",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "e2e:dist": "start-test serve 8000 'cypress open --config baseUrl=http://localhost:8000'"
  },
  "dependencies": {
    "@nuxt/content": "^1.9.0",
    "cypress": "^6.4.0",
    "nuxt": "^2.14.6",
    "remark-gfm": "^1.0.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.2",
    "local-web-server": "4.2.1",
    "netlify-plugin-cypress": "^1.7.0",
    "start-server-and-test": "1.12.0"
  }
}
